# Iterations per epoch. null => use dataloader length.
ipe: null
ipe_scale: 1.0

# Training length.
epochs: 100

# AdamW params.
lr: 1.5e-4
start_lr: 1.0e-6
final_lr: 1.0e-6
warmup: 0.1
weight_decay: 0.04
final_weight_decay: 0.4
betas: [0.9, 0.95]
eps: 1.0e-8
clip_grad: null
